<%= bootstrap_form_for(@tag) do |f| %>

  <%= f.alert_message "Please review the problems below:" %>

  <%= f.text_field :keyword %>

  <%= f.primary %>
  <%= link_to "Back", tags_path, class: "btn btn-link" %>

<% end %>
