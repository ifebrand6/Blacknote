<% provide(:title) do %>
  Note #<%= @note.id %>
<% end %>

<table class="table table-bordered">
  <tbody>
    <tr>
      <th class="col-sm-2">Title</th>
      <td><%= @note.title %></td>
    </tr>
    <tr>
      <th class="col-sm-2">Content</th>
      <td><%= @note.content %></td>
    </tr>
  </tbody>
</table>

<p>
  <%= link_to "Edit", edit_category_note_path(@category), class: "btn btn-primary" %>
  <%= link_to "Back", category_notes_path(@category), class: "btn btn-link" %>
</p>
